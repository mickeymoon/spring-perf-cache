spring.datasource.url=jdbc:mysql://localhost:3306/cache
spring.sql.init.mode=always

# Load credentials and other overrides from an external properties file.
# Copy config/datasource.properties.example to config/datasource.properties and
# fill in the appropriate values for your environment.
spring.config.import=optional:file:./config/datasource.properties
spring.redis.host=localhost
spring.redis.port=6379
